{
  "name": "ringabell",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest run --project unit",
    "test:unit": "vitest run --project unit",
    "test:seed": "wrangler d1 migrations apply ringabell --local && wrangler d1 execute ringabell --local --file test/fixtures/seed.sql",
    "test:integration": "nuxi build && npm run test:seed && vitest run --project integration",
    "test:all": "nuxi build && vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@nuxt/eslint": "^1.15.2",
    "dayjs": "^1.11.20",
    "eslint": "^10.4.1",
    "nuxt": "^4.4.6",
    "nuxt-auth-utils": "^0.5.29",
    "vue": "^3.5.34",
    "vue-router": "^5.0.7"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.11",
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.96.0"
  }
}
