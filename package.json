{
  "name": "ringabell",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest run --project unit",
    "test:unit": "vitest run --project unit",
    "test:integration": "nuxi build && vitest run --project integration",
    "test:all": "nuxi build && vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dayjs": "^1.11.20",
    "nuxt": "^4.4.6",
    "nuxt-auth-utils": "^0.5.29",
    "vue": "^3.5.34",
    "vue-router": "^5.0.7"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.11",
    "vitest": "^4.1.8",
    "wrangler": "^4.96.0"
  }
}
